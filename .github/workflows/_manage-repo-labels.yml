---
name: Manage Repo Labels

on:
  push:
    branches:
      - main
    paths:
      - .github/repo_labels.yml
      - .github/workflows/manage-repo-labels.yml

jobs:
  manage-repo-labels:
    name: Manage Repo Labels
    uses: ./.github/workflows/__manage-repo-labels.yml
